# Supabase (프로젝트 설정 > API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Drizzle 마이그레이션용 DB 접속 문자열 (Supabase: Settings > Database > Connection string, pooler)
DATABASE_URL=

# 카카오 OAuth 는 Supabase Auth > Providers 에서 설정 (앱 키/redirect URL)
